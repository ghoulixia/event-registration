services:
  postgres:
    image: postgres:16
    container_name: event-registration-postgres
    environment:
      POSTGRES_DB: event_registration
      POSTGRES_USER: event
      POSTGRES_PASSWORD: event123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: